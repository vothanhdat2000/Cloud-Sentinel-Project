# Cloud Sentinel - Environment template
# Copy to .env and adjust. Never commit .env to git.

# LocalStack / AWS (for local dev)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1
LOCALSTACK_ENDPOINT=http://localhost:4566

# Audit report path (optional; notifier uses this)
# SENTINEL_REPORT_PATH=./infra/audit_report.json

# Optional: Slack webhook for alerts (leave empty to use console only)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# Optional: Microsoft Teams webhook for alerts
# TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/...
